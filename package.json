{
  "name": "souvenir",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test:client": "vitest run --root . test/client",
    "test:e2e": "playwright test",
    "test": "npm run test:client && npm run test:e2e",
    "screenshots": "node scripts/capture-screenshots.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
